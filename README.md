For running pre trained model (example, qwen2.5-transformers-0.5b-instruct-gptq-int4-v1)

1. First download the tar.gz file of the model(or zip) and unzip.
2. Install the dependencies (example pip install transformers accelerate torch datasets)

